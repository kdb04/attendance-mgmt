# attendance-mgmt

- run backend server:
```
cd backend/AttendanceManagementSystem
mvn spring-boot:run
```
open ```localhost:8080```

- run frontend server:
```
cd frontend
npm install -y
npm run dev
```
